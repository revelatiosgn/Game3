using System.Collections;
using System.Collections.Generic;
using UnityEngine;

using ARPG.Movement;
using ARPG.Items;
using ARPG.Gear;

namespace ARPG.Gear
{
    public class ShieldBehaviour : MonoBehaviour
    {
        // private int defenceLayerIndex;

        // public override void Initialize(GameObject gameObject, EquipmentWeaponSlot slot)
        // {
        //     base.Initialize(gameObject, slot);

        //     // defenceLayerIndex = animator.GetLayerIndex(item.animationLayer + "Defence");
        //     // animator.SetLayerWeight(defenceLayerIndex, 1f);
        //     // animator.SetBool("shield", true);
        // }

        // public override void Dispose()
        // {
        //     base.Dispose();

        //     // animator.SetLayerWeight(defenceLayerIndex, 0f);
        //     // animator.SetBool("shield", false);
        // }
    }
}
